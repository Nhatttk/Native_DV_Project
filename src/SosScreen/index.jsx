import React from 'react';
import { SafeAreaView, StyleSheet, Text, View } from 'react-native';

const SosScreen = ({ navigation }) => {
    return (
        <SafeAreaView>
            <View>
            <Text>Help right way</Text>
            </View>
        </SafeAreaView>
    );
}

const styles = StyleSheet.create({})

export default SosScreen;
